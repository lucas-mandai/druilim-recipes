import { styled } from '@/styles'

export const FooterContainer = styled('footer', {
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  alignItems: 'center',
  padding: '20px 0',
})
